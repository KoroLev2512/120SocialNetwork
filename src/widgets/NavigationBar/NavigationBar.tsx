import React, {useMemo} from "react";

export const NavigationBar = () => {
    return (
        <main className="flex flex-col items-end min-w-[50px] bg-amber-300">
            vfvs
        </main>
    );
}
