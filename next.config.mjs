/** @type {import('next').NextConfig} */

const nextConfig = {
    // trailingSlash: true,
    // output: "standalone",
    // reactStrictMode: false,
    // swcMinify: true,
};


export default nextConfig;
