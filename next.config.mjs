/** @type {import('next').NextConfig} */

const nextConfig = {
    images: {
        domains: ['i.pinimg.com']
    }
    // trailingSlash: true,
    // output: "standalone",
    // reactStrictMode: false,
    // swcMinify: true,
};


export default nextConfig;
